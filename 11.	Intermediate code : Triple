#include <stdio.h>

int add(int a, int b) {
    return a + b;
}

int multiply(int a, int b) {
    return a * b;
}

int main() {
    int x = 5;
    int y = 10;
    int z;

    z = add(x, y);
    printf("x + y = %d\n", z);

    z = multiply(x, y);
    printf("x * y = %d\n", z);

    return 0;
}
